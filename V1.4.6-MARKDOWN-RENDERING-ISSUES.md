# MD Viewer v1.4.6 - Markdown 渲染问题分析与解决方案

> **创建时间**: 2026-01-12
> **问题来源**: 用户反馈三个渲染问题
> **目标版本**: v1.4.6

---

## 📋 问题清单

### 问题 1: Marp 格式支持 ❌
**文件**: `王健2025年度工作总结-Marp版.md`
**问题**: Marp 的 YAML Front Matter 和特殊指令无法正确渲染
**严重程度**: 🟡 中等

### 问题 2: 表格内 `<br>` 标签渲染异常 🔴
**文件**: `王健2025年度工作总结-PPT汇报版.md`
**问题**: 表格中的 `<br>` 换行符没有正确渲染
**严重程度**: 🔴 高

### 问题 3: 原生 HTML `<table>` 标签渲染失败 🔴
**文件**: `王健2025年度工作总结-优化版.md`
**问题**:
- 原生 HTML `<table>` 标签显示为源码
- Mermaid Sankey 图表配置代码直接显示
**严重程度**: 🔴 高

---

## 🔍 问题详细分析

### 问题 1: Marp 格式不支持

#### 问题描述
```markdown
---
marp: true
theme: default
size: 16:9
paginate: true
header: '2025年度工作总结'
footer: '王健 | 研发中心副总监'
style: |
  section {
    font-family: 'Arial', sans-serif;
  }
---

<!-- _paginate: false -->
<!-- _header: '' -->
```

**现象**:
- YAML Front Matter 显示为普通文本
- Marp 特殊指令（如 `<!-- _paginate: false -->`）不生效
- 无幻灯片分页功能

#### 根本原因
1. **markdown-it 不支持 Marp**：
   - Marp 是专门的 Markdown 演示文稿工具
   - 使用 Marpit 作为核心渲染引擎
   - MD Viewer 使用 markdown-it，两者不兼容

2. **架构冲突**：
   - Marp 需要特定的渲染管道和样式系统
   - 与 MD Viewer 的 VirtualizedMarkdown 架构不兼容

#### 可行性评估
**❌ 不建议支持 Marp**

**原因**:
1. **技术成本高**:
   - 需要集成完整的 Marpit 引擎（~500KB）
   - 与现有 markdown-it 管道冲突
   - 需要重写渲染逻辑

2. **维护成本高**:
   - Marp 生态独立，更新频繁
   - 需要长期维护兼容性

3. **用户需求低**:
   - Marp 用户应该使用 Marp CLI 或 VS Code Marp 插件
   - MD Viewer 的定位是 **通用 Markdown 预览器**，不是演示文稿工具

**替代方案**:
- 建议用户使用官方 Marp 工具：
  - [Marp CLI](https://github.com/marp-team/marp-cli)
  - [VS Code Marp 插件](https://marketplace.visualstudio.com/items?itemName=marp-team.marp-vscode)

---

### 问题 2: 表格内 `<br>` 标签不渲染

#### 问题描述
```markdown
| 阶段 | 时间 | 关键里程碑 |
|------|------|----------|
| **Q1<br>安全与架构升级** | 1-3月 | • 持续迭代建设<br>• 火山防火墙集成 |
```

**期望**:
```
Q1
安全与架构升级
```

**实际**:
```
Q1<br>安全与架构升级
```

#### 根本原因
1. **DOMPurify 配置问题**：
   ```typescript
   // src/renderer/src/utils/markdown.ts
   const clean = DOMPurify.sanitize(dirty, {
     ADD_TAGS: ['iframe'],
     ADD_ATTR: ['allow', 'allowfullscreen', 'frameborder', 'scrolling'],
     ALLOWED_URI_REGEXP: /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|xxx):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i
   })
   ```
   - `<br>` 标签被 DOMPurify 默认允许
   - 但在某些上下文（如表格单元格）可能被转义

2. **markdown-it 表格渲染**：
   - markdown-it-attrs 插件可能干扰
   - 表格内联 HTML 处理逻辑有 bug

#### 解决方案
**✅ 可以修复**

**方案 A: 修复 DOMPurify 配置（推荐）**

```typescript
// src/renderer/src/utils/markdown.ts

const clean = DOMPurify.sanitize(dirty, {
  ADD_TAGS: ['iframe'],
  ADD_ATTR: ['allow', 'allowfullscreen', 'frameborder', 'scrolling'],
  ALLOWED_URI_REGEXP: /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|xxx):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i,
  // 新增：确保表格内的 <br> 标签不被转义
  KEEP_CONTENT: true,
  FORCE_BODY: false
})
```

**方案 B: 预处理表格内容**

```typescript
// 在 markdown-it 渲染前预处理
function preprocessTableBr(markdown: string): string {
  // 匹配表格行
  return markdown.replace(/\|([^|\n]+)<br>([^|\n]+)\|/g, (match, before, after) => {
    return `|${before}\n${after}|`
  })
}
```

**实施工期**: 0.5 天

---

### 问题 3: 原生 HTML `<table>` 标签显示为源码

#### 问题描述
**场景 1: HTML Table**
```html
<table>
<tr>
<td width="33%">
  内容
</td>
</tr>
</table>
```

**显示为**:
```
<table>
<tr>
<td width="33%">
```

**场景 2: Mermaid Sankey 配置显示**
```markdown
### 💰 外部项目金额流向图

```mermaid
%%{init: {'theme':'base', 'themeVariables': { 'fontSize':'16px'}}}%%
sankey-beta

外部项目172万+,智能化项目群,92
```
```

**显示为**:
```
%%{init: {'theme':'base', 'themeVariables': { 'fontSize':'16px'}}}%%
sankey-beta

外部项目172万+,智能化项目群,92
```

#### 根本原因

**场景 1: HTML Table 问题**

1. **markdown-it 配置**：
   ```typescript
   // src/renderer/src/utils/markdown.ts
   export const md = markdownit({
     html: true,  // ✅ 已启用 HTML
     breaks: true,
     linkify: true
   })
   ```
   - `html: true` 已启用，应该支持
   - 但可能被 DOMPurify 过滤

2. **DOMPurify 过度过滤**：
   ```typescript
   const clean = DOMPurify.sanitize(dirty, {
     ADD_TAGS: ['iframe'],  // ⚠️ 没有显式允许 table 标签
     // ...
   })
   ```
   - DOMPurify 默认允许 `<table>`、`<tr>`、`<td>`
   - 但可能因为嵌套或属性（如 `width`）被过滤

**场景 2: Mermaid Sankey 问题**

1. **Mermaid 版本问题**：
   - Sankey 图表是 Mermaid 11+ 的新特性（`sankey-beta`）
   - 当前项目可能使用旧版 Mermaid

2. **初始化配置缺失**：
   ```typescript
   // 可能缺少 Sankey 图表的配置
   mermaid.initialize({
     startOnLoad: false,
     theme: 'default',
     // ⚠️ 缺少 Sankey 配置
   })
   ```

#### 解决方案

**✅ 可以修复**

**方案 A: 修复 DOMPurify 配置（推荐）**

```typescript
// src/renderer/src/utils/markdown.ts

export function sanitizeHtml(dirty: string): string {
  return DOMPurify.sanitize(dirty, {
    // 显式允许常用 HTML 标签
    ADD_TAGS: ['iframe', 'table', 'thead', 'tbody', 'tr', 'th', 'td'],

    // 允许常用属性
    ADD_ATTR: [
      'allow', 'allowfullscreen', 'frameborder', 'scrolling',
      'width', 'height', 'colspan', 'rowspan', 'align', 'valign',
      'style', 'class'
    ],

    ALLOWED_URI_REGEXP: /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|xxx):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i,

    // 保留内容结构
    KEEP_CONTENT: true,
    RETURN_DOM_FRAGMENT: false,
    RETURN_DOM: false
  })
}
```

**方案 B: 升级 Mermaid 并配置 Sankey**

```typescript
// 1. 升级 Mermaid
// package.json
{
  "dependencies": {
    "mermaid": "^11.4.0"  // 升级到最新版本
  }
}

// 2. 配置 Sankey 图表
// src/renderer/src/components/VirtualizedMarkdown.tsx

useEffect(() => {
  mermaid.initialize({
    startOnLoad: false,
    theme: 'default',

    // 新增 Sankey 配置
    sankey: {
      width: 600,
      height: 400,
      linkColor: 'gradient',
      nodeAlignment: 'justify'
    }
  })
}, [])
```

**实施工期**: 1 天

---

## 📊 优先级评估

| 问题 | 严重程度 | 用户影响 | 技术难度 | 工期 | 优先级 |
|------|---------|---------|---------|------|--------|
| Marp 支持 | 🟡 中 | 低（专用工具） | 高 | 3-5天 | **P3（不建议）** |
| 表格 `<br>` | 🔴 高 | 高（常见场景） | 低 | 0.5天 | **P0（必须修复）** |
| HTML Table | 🔴 高 | 高（常见场景） | 中 | 1天 | **P0（必须修复）** |

---

## ✅ v1.4.6 实施计划

### 方案建议

**包含**:
1. ✅ 修复表格内 `<br>` 标签渲染（P0）
2. ✅ 修复原生 HTML `<table>` 标签渲染（P0）
3. ✅ 升级 Mermaid 并支持 Sankey 图表（P1）

**不包含**:
- ❌ Marp 格式支持（建议用户使用专用工具）

### 技术方案

#### 1. 修复 DOMPurify 配置

**文件**: `src/renderer/src/utils/markdown.ts`

```typescript
export function sanitizeHtml(dirty: string): string {
  return DOMPurify.sanitize(dirty, {
    // 允许常用 HTML 标签
    ADD_TAGS: [
      'iframe',
      // 表格相关
      'table', 'thead', 'tbody', 'tfoot', 'tr', 'th', 'td', 'caption',
      // 布局相关
      'div', 'span', 'br', 'hr'
    ],

    // 允许常用属性
    ADD_ATTR: [
      // iframe
      'allow', 'allowfullscreen', 'frameborder', 'scrolling',
      // 表格
      'width', 'height', 'colspan', 'rowspan', 'align', 'valign',
      // 通用
      'style', 'class', 'id'
    ],

    // 允许 style 属性的 CSS 属性
    ALLOWED_STYLES: {
      '*': {
        'width': [/^\d+%$/],
        'height': [/^\d+%$/],
        'text-align': [/^(left|right|center|justify)$/],
        'vertical-align': [/^(top|middle|bottom)$/]
      }
    },

    ALLOWED_URI_REGEXP: /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|xxx):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i,

    // 保留内容结构
    KEEP_CONTENT: true
  })
}
```

#### 2. 升级 Mermaid

**文件**: `package.json`

```json
{
  "dependencies": {
    "mermaid": "^11.4.0"
  }
}
```

**文件**: `src/renderer/src/components/VirtualizedMarkdown.tsx`

```typescript
useEffect(() => {
  mermaid.initialize({
    startOnLoad: false,
    theme: 'default',
    securityLevel: 'loose',

    // 新增 Sankey 配置
    sankey: {
      width: 600,
      height: 400,
      linkColor: 'gradient',
      nodeAlignment: 'justify',
      useMaxWidth: true
    }
  })
}, [])
```

#### 3. 测试用例

**文件**: `src/renderer/test/markdown-rendering.test.tsx`

```typescript
describe('Markdown Rendering - v1.4.6', () => {
  it('应该正确渲染表格内的 <br> 标签', () => {
    const markdown = `
| 列1 | 列2 |
|-----|-----|
| A<br>B | C<br>D |
`
    const html = renderMarkdown(markdown)
    expect(html).toContain('<br>')
    expect(html).not.toContain('&lt;br&gt;')
  })

  it('应该正确渲染原生 HTML table', () => {
    const markdown = `
<table>
<tr>
<td width="50%">内容</td>
</tr>
</table>
`
    const html = renderMarkdown(markdown)
    expect(html).toContain('<table>')
    expect(html).toContain('<td width="50%">')
    expect(html).not.toContain('&lt;table&gt;')
  })

  it('应该正确渲染 Mermaid Sankey 图表', async () => {
    const markdown = `
\`\`\`mermaid
%%{init: {'theme':'base'}}%%
sankey-beta

A,B,10
B,C,5
\`\`\`
`
    const wrapper = mount(<VirtualizedMarkdown content={markdown} />)
    await waitFor(() => {
      expect(wrapper.find('.mermaid').exists()).toBe(true)
      expect(wrapper.find('svg').exists()).toBe(true)
    })
  })
})
```

### 实施步骤

1. **修复 DOMPurify 配置** (2 小时)
   - 修改 `src/renderer/src/utils/markdown.ts`
   - 添加允许的标签和属性
   - 测试表格和 HTML 渲染

2. **升级 Mermaid** (3 小时)
   - 升级到 Mermaid 11.4.0
   - 配置 Sankey 图表支持
   - 测试所有 Mermaid 图表类型

3. **编写测试** (2 小时)
   - 添加表格 `<br>` 测试
   - 添加 HTML table 测试
   - 添加 Mermaid Sankey 测试

4. **回归测试** (1 小时)
   - 运行所有测试（402 个）
   - 手动测试三个文档
   - 确保无性能回归

5. **文档更新** (1 小时)
   - 更新 CHANGELOG.md
   - 更新 README.md
   - 更新 PROGRESS.md

**总工期**: 9 小时（1.5 个工作日）

---

## 🎯 预期结果

### 修复前
| 问题 | 状态 |
|------|------|
| 表格内 `<br>` | ❌ 显示为文本 |
| HTML `<table>` | ❌ 显示为源码 |
| Mermaid Sankey | ❌ 显示配置代码 |

### 修复后
| 问题 | 状态 |
|------|------|
| 表格内 `<br>` | ✅ 正确换行 |
| HTML `<table>` | ✅ 正确渲染表格 |
| Mermaid Sankey | ✅ 正确渲染图表 |

---

## 📝 备注

### Marp 不支持的说明

在 README.md 中添加说明：

```markdown
## 不支持的格式

MD Viewer 专注于通用 Markdown 渲染，以下格式不支持：

### Marp 演示文稿
Marp 是专门的演示文稿工具，请使用官方工具：
- [Marp CLI](https://github.com/marp-team/marp-cli)
- [VS Code Marp 插件](https://marketplace.visualstudio.com/items?itemName=marp-team.marp-vscode)
```

### 测试文件清单

测试时使用以下文件验证：
1. `/Users/mac/Documents/temp/youxiang/王健2025年度工作总结-PPT汇报版.md` (表格 `<br>`)
2. `/Users/mac/Documents/temp/youxiang/王健2025年度工作总结-优化版.md` (HTML table + Sankey)
3. `/Users/mac/Documents/temp/youxiang/王健2025年度工作总结-Marp版.md` (文档说明不支持)

---

**创建时间**: 2026-01-12
**作者**: Claude
**状态**: 待实施
